---
framework: jest     
preRun:
  command:
    - yarn install          
postMerge:
  pattern: 
    - "./app/**/*.test.ts"
    - "./app/**/*.spec.ts"
preMerge:
  pattern:
    - "./app/**/*.test.ts"
    - "./app/**/*.spec.ts"
postRun:
  command:
    - yarn --version
version: 1.0
